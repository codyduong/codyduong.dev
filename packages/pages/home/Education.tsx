import Section from 'packages/components/Section/Section';

export default function Education(): JSX.Element {
  return (
    <Section title={'Education'} subtitle="School and shiz">
      womp
    </Section>
  );
}
